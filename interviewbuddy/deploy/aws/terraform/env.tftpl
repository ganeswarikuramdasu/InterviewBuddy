# Rendered by Terraform and prepended to user-data.sh. Only plain
# ${...} Terraform placeholders live here (see main.tf -> local.user_data).
export GIT_REPO="${git_repo}"
export GIT_BRANCH="${git_branch}"
export GIT_TOKEN="${git_token}"
export S3_BUNDLE_URL="${s3_bundle_url}"
export DB_HOST="${db_host}"
export DB_PORT="${db_port}"
export DB_NAME="${db_name}"
export DB_USERNAME="${db_username}"
export DB_PASSWORD="${db_password}"
export DB_SSLMODE="${db_sslmode}"
export JWT_SECRET="${jwt_secret}"
export GEMINI_API_KEY="${gemini_api_key}"
export CODE_EXECUTION_SERVICE_URL="${code_execution_service_url}"
export FRONTEND_PORT="${frontend_port}"
